---
- name: Ensure the APT keyring directory exists
  ansible.builtin.file:
    path: /etc/apt/keyrings
    state: directory
    mode: "0755"
  when: common_enable_vscode or common_enable_brave
  tags:
    - repos

- name: Ensure the GPG keyrings directory exists
  ansible.builtin.file:
    path: /usr/share/keyrings
    state: directory
    mode: "0755"
  when: common_enable_vscode
  tags:
    - repos
    - vscode

- name: Download Microsoft APT signing key (ASCII)
  ansible.builtin.get_url:
    url: https://packages.microsoft.com/keys/microsoft.asc
    dest: /tmp/microsoft.asc
    mode: "0644"
  when: common_enable_vscode
  tags:
    - repos
    - vscode

- name: Convert Microsoft APT signing key to GPG keyring format
  ansible.builtin.command:
    cmd: gpg --dearmor --yes --output /usr/share/keyrings/microsoft.gpg /tmp/microsoft.asc
    creates: /usr/share/keyrings/microsoft.gpg
  when: common_enable_vscode
  tags:
    - repos
    - vscode

- name: Clean up temporary Microsoft APT signing key file
  ansible.builtin.file:
    path: /tmp/microsoft.asc
    state: absent
  when: common_enable_vscode
  tags:
    - repos
    - vscode

- name: Remove any existing vscode.sources file to avoid duplicate repository definitions
  ansible.builtin.file:
    path: /etc/apt/sources.list.d/vscode.sources
    state: absent
  when: common_enable_vscode
  tags:
    - repos
    - vscode

- name: Register the VS Code repository
  ansible.builtin.apt_repository:
    repo: >-
      deb [arch={{ common_deb_arch }} signed-by=/usr/share/keyrings/microsoft.gpg]
      https://packages.microsoft.com/repos/code stable main
    filename: vscode
    state: present
    update_cache: true
  when: common_enable_vscode
  tags:
    - repos
    - vscode

- name: Download Brave APT signing key
  ansible.builtin.get_url:
    url: https://brave-browser-apt-release.s3.brave.com/brave-browser-archive-keyring.gpg
    dest: /etc/apt/keyrings/brave-browser-archive-keyring.gpg
    mode: "0644"
  when: common_enable_brave
  tags:
    - repos
    - brave

- name: Register the Brave repository
  ansible.builtin.apt_repository:
    repo: >-
      deb [signed-by=/etc/apt/keyrings/brave-browser-archive-keyring.gpg arch={{ common_deb_arch }}]
      https://brave-browser-apt-release.s3.brave.com/ stable main
    filename: brave-browser-release
    state: present
    update_cache: true
  when: common_enable_brave
  tags:
    - repos
    - brave

- name: Ensure the Docker APT keyring directory exists
  ansible.builtin.file:
    path: /etc/apt/keyrings
    state: directory
    mode: "0755"
  when: common_enable_docker
  tags:
    - repos
    - docker

- name: Download Docker APT signing key
  ansible.builtin.get_url:
    url: https://download.docker.com/linux/ubuntu/gpg
    dest: /etc/apt/keyrings/docker.gpg
    mode: "0644"
  when: common_enable_docker
  tags:
    - repos
    - docker

- name: Register the Docker repository
  ansible.builtin.apt_repository:
    repo: >-
      deb [arch={{ common_deb_arch }} signed-by=/etc/apt/keyrings/docker.gpg]
      https://download.docker.com/linux/ubuntu {{ ansible_lsb.codename }} stable
    filename: docker
    state: present
    update_cache: true
  when: common_enable_docker
  tags:
    - repos
    - docker
